# Play Miyazaki

宮崎で「遊ぶ」ための専門一覧サイト（ディレクトリサイト）。サーフィン・キャンプ／車中泊・食・ゴルフの4ジャンルについて、宮崎県内のお店・施設・スポットを「そのジャンルなら日本一くわしい一覧」として載せる。日本語が正、英語版を同時に出す。

**最上位の指針は `docs/要件.md`。実装・意思決定の前に必ず参照すること。**
- `docs/要件.md` … 何を作るか、やらないこと、決めたことと理由

# 中心思想

**網羅性が価値。** 「宮崎の○○を探すなら、ここを見れば全部ある」状態を作る。おすすめ10選ではなく全件一覧。

言い換え（設計判断の前提として扱う）：
- 1件の紹介文の出来より、抜けが無いことのほうが大事
- 事実だけを載せる。盛らない、創作しない、推測しない
- 運営者（じゅん）の作業はゼロに近づける。判断以外の作業は全部コードと自動化で

# 用語（実装と会話で統一）

`listing` = 一覧に載る1件（店・施設・スポット）。`data/listings/<genre>/<slug>.json`。UI表記は「スポット」。同じ敷地でも、役割と連絡先が別なら別 listing にしてよい（例：道の駅の物産館とキャンプ場）
`genre` = ジャンル。`surf` / `camp` / `food` / `golf` の4つ固定。UI表記は「サーフィン」「キャンプ・車中泊」「食」「ゴルフ」
`category` = ジャンル内の種別（例：surf の `shop` / `school` / `rental` / `point` / `stay`）。UI表記は「種類」
`area` = 市町村（例：宮崎市・日向市・都城市）。UI表記は「エリア」
`source` = その listing の情報の出典URL。**必須。無い listing は存在してはいけない**
`verified_at` = 出典を最後に確認した日付（YYYY-MM-DD）
`feature` = 出典で確認できる客観的な特徴（`data/taxonomy.json` の features）。UI表記は「特徴」。主観（絶景・星空がきれい等）は feature にしない

「レビュー」「口コミ」「予約」はユーザーに見せる言葉としては存在しない（機能として作らないため）。

# データの source of truth

| 概念 | どこが正 | 備考 |
|---|---|---|
| listing の全項目 | `data/listings/<genre>/<slug>.json` のみ | DB は無い。ページはビルド時に JSON から生成する |
| 英語テキスト | 同じ JSON の `en` フィールド | 閲覧時に翻訳APIを呼ばない(課金と遅延を避ける) |
| 営業時間・料金 | 出典URLに書いてある値のみ | 出典に無ければ `null` にして UI は「公式サイトで確認」と表示 |
| `price` の書き方 | 出典に書いてある金額をそのまま短く要約した1〜2行 | 例：「サイト利用 3,300円〜／1区画、入場料 大人500円・小人300円。」「1泊 3,000円／台（電源込み）。」「大人 600円、小人 300円。」。金額は出典の表記に合わせる（税込・税別が書いてあればそれも書く）。「〜」は出典に幅があるときだけ。「約」「目安」は使わない。出典に料金が無い・前年度以前の料金しか無い・PDFで読めない場合は `null` のまま。`price` を書き換えた listing は `verified_at` を当日に更新する。料金ページが同じ公式サイトの別URLにあるときは、そのURLを `sources` に追加してよい |
| ジャンル・種別の定義 | `data/taxonomy.json` | ここに無い genre / category を JSON に書かない |
| listing の features | 各 JSON の `features` 配列 | `scripts/patch-features.ts` は UNION（追加のみ）で付ける。外すときは JSON を手で直す |

# North Star Metric

**Google 検索からの月間クリック数（Search Console）。** これ以外で成功判断しない。PV や訪問者数は見ない（自分のアクセスや bot で動くため）。

# 設計原則

- **静的生成（SSG）で全ページを出す**（検索エンジンに強く、サーバー費用ゼロ。DB が無いので可能）
- **1 listing = 1 ページ、ジャンル×種別、ジャンル×エリアの一覧ページを持つ**（検索クエリ「宮崎 サーフィン スクール」「日向市 キャンプ場」に1ページずつ当てるため）
- **日本語 `/ja/...` と英語 `/en/...` を別URLにし、`hreflang` を付ける**（検索エンジンに別ページとして認識させるため）
- **写真を使わない**（他人の写真は権利問題、AI生成は実在の店の写真と誤解される。地図リンクと文字で勝負）
- **外部リンクは公式サイト → Google マップ の順で必ず置く**（予約サイトへのリンクは第2段階で紹介リンクに差し替える。今は素のリンク）
- **listing の追加はスクリプトで行う**（`scripts/add-listing.ts` に出典URLを渡すと JSON の雛形を作る。手書きで JSON を作らない — 項目の抜けと表記ゆれの原因になる）
- **見た目の判断は `docs/design.md` が唯一の根拠。色・フォント・動き・部品はそこに書かれたものだけを使う**
- **検索意図ごとに1ページ：ジャンル×特徴の条件ページ（`/[lang]/[genre]/f/[feature]`）を持つ。該当3件以上のときだけ生成する（薄いページを作らない）**

# 現在のフェーズ

**「第1段階：サーフィンの網羅と公開」。**
最大のリスクは、件数が少ないまま公開して「10選ブログ」と同じに見えること。サーフィンジャンルは宮崎県内のショップ・スクール・レンタル・ポイント・宿を **出典付きで50件以上** 集めてから公開する。収益機能はまだ作らない。

# 行動原則（迷ったらここに従う）

1. 出典URLが無い情報は書かない。分からない項目は `null`
2. 迷ったら機能を増やさず、listing を増やす
3. 文章は短く事実だけ。「絶景」「最高」「おすすめ」などの評価語は使わない
4. 日本語を先に書き、英語はそれを訳す。英語だけにある情報を作らない
5. 新しい genre / category / 項目 を足したくなったら、足さずに報告して止まる

# 今は作らないもの

- ユーザー登録・ログイン（今はやらない。第3段階で事業者用だけ）
- 口コミ・レビュー・評価（永久にやらない）
- 予約機能・問い合わせフォーム（永久にやらない。予約サイトへ送る側に立つ）
- 事業者への自動メール・問い合わせ（永久にやらない）
- 紹介リンク（アフィリエイト）（今はやらない。第2段階。アカウント開設はじゅんさんが行う）
- 有料掲載枠・決済（今はやらない。第3段階）※ 見た目（おすすめ枠の行の色・ラベル）だけは design.md に定義済み。機能は作らない
- 広告（今はやらない。第4段階）
- 写真・画像（今はやらない）
- データベース（今はやらない。有料枠の段階で検討）
- ブログ・記事コンテンツ（今はやらない。一覧が揃うまで書かない）
- アプリ（永久にやらない）
- 地図の埋め込み（今はやらない。lat/lng が揃ったら Leaflet + OpenStreetMap（APIキー不要・無料）で検討。Google Maps API は課金の芽なので使わない）
- ランキング・人気順（永久にやらない。実データが無いのに「人気」と書くのは創作にあたる）
**理由：じゅんさんの稼働ゼロと費用ゼロを最優先。網羅性が付く前に機能を足しても検索流入は増えない**（2026年9月）

# 未決定の警戒線（仮説を実装ルールにしない）

以下は **検討中であって決定事項ではない**。仕様として実装しない。
- 有料掲載枠の価格（月3,000〜5,000円は目安）
- 紹介リンクにどの ASP を使うか（A8 / バリューコマース / 楽天アフィリエイト直）
- 5つ目以降のジャンル
- 都城市を「宮崎市から車で」の導線として特別扱いするかどうか

# 絶対にやってはいけないこと

- 架空の店・スポット・住所を載せる。出典が確認できない情報を「事実」として出す
- 出典に無い営業時間・料金・定休日を推測で書く
- 事業者に自動でメール・フォーム送信・電話予約をする
- 課金が発生する操作（有料API、Vercel Pro、有料プラグイン等）
- `git push --force`、`rm -rf`、`data/` 配下の一括削除
- 既存の listing JSON を「整理」の名目で削除する（消すときは `status: "closed"` にする）

# 環境

- リポジトリ：`pubhub2245/playmiyazaki` / 本番ブランチ：`main`
- 本番URL：https://playmiyazaki.com（Vercel。プレビューは `playmiyazaki.vercel.app`）
- 起動：`cd C:\projects\playmiyazaki` → `claude`
- 技術構成：Next.js 14（App Router, SSG）+ TypeScript + Tailwind CSS。DB 無し。Windows 環境
- フォントは `next/font/google` で自己ホスト（ビルド時に取り込むので閲覧時の外部リクエストは無い）。STEP 1 の「システムフォント、Google Fonts を読み込まない」はこの方針で上書き（閲覧時に外部へ取りに行かないので速度も費用も変わらないため）
- **作業前に `git fetch` すること。** ローカルが古いと `git status` は「同期済み」と誤報告する（比較先が最後に fetch した時点のスナップショットのため）

# 声

地元の人が友達に「ここ行っとけ」と教える口調。短く、事実だけ、盛らない。例：「木崎浜は初心者向けの波が多い。駐車場は無料、シャワーは有料。」
英語も同じ温度：「Kizakihama has beginner-friendly waves most of the year. Free parking, paid showers.」
